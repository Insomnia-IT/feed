## План по групповым бейджам

Экран "План по групповым бейджам" отображает информацию о планируемом количестве питаний по каждому групповому бейджу на текущий приём пищи.

### Расположение

Экран доступен из раздела "История" через кнопку "План по групповым бейджам", которая находится под кнопкой "Статистика кормлений".

### Структура данных

На экране отображается таблица с четырьмя колонками:

1. **Название** — название группового бейджа
2. **Служба** — название службы (direction), к которой относится групповой бейдж
3. **План** — планируемое количество питаний на текущий приём пищи (разбивка: мясоеды/веганы)
4. **Факт** — фактическое количество выданных питаний на текущий приём пищи (разбивка: мясоеды/веганы)

В конце таблицы отображается строка **Итого** с суммарными значениями плана и факта по всем групповым бейджам.

Значения в колонках "План" и "Факт" отображаются в формате `<мясоеды>/<веганы>`, где:

- Мясоеды отображаются красным цветом
- Веганы отображаются зелёным цветом

### Логика расчёта плана

План для каждого группового бейджа рассчитывается следующим образом:

1. Система ищет ячейку планирования (`MealPlanCell`) для текущего приёма пищи (`mealTime`) и текущей даты
2. Если ячейка найдена:
    - План (мясоеды) = `amount_meat`
    - План (веганы) = `amount_vegan`
3. Если ячейка не найдена:
    - План (мясоеды) = количество волонтёров с этим групповым бейджем, находящимся на поле
    - План (веганы) = 0

### Логика расчёта факта

Факт для каждого группового бейджа рассчитывается как сумма транзакций за текущий приём пищи:

- Факт (мясоеды) = сумма транзакций с `is_vegan = false`
- Факт (веганы) = сумма транзакций с `is_vegan = true`

### Архитектура компонента

Компонент состоит из следующих частей:

- **`use-group-badge-plan-stats.ts`** — хук, который получает данные о групповых бейджах и волонтёрах, рассчитывает план и факт для каждого бейджа
- **`group-badge-plan.tsx`** — основной компонент экрана
- **`group-badge-plan-table.tsx`** — компонент таблицы для отображения данных
- **`group-badge-plan-screen.tsx`** — экран с заголовком и навигацией

### Фильтрация данных

Отображаются только групповые бейджи, которые:

- Не отключены (`is_disabled = false`)
- Не удалены (`deleted_at = null`)
- Принадлежат текущей кухне или являются общими (`kitchen = null`)
- Имеют план > 0 ИЛИ факт > 0

### Пример использования

```typescript
import { useGroupBadgePlanStats } from 'shared/hooks/use-group-badge-plan-stats';

const { stats } = useGroupBadgePlanStats();
// stats: Array<{
//   id: number;
//   name: string;
//   direction: string | null;
//   planMeat: number;
//   planVegan: number;
//   factMeat: number;
//   factVegan: number
// }>
```

### Связанные документы

- [Статистика кормлений](./feeding-stats.md) — экран статистики кормлений
- [Групповые бейджи](./group-badges.md) — информация о групповых бейджах
