name: Lint

on:
  pull_request:

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v3
    - name: yarn install
      run: yarn --frozen-lockfile
    - name: lint
      run: yarn lint
    - name: tc
      run: yarn tc
